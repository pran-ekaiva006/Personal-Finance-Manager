{
  "name": "server",
  "version": "1.0.0",
  "main": "start.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node start.js",
    "dev": "nodemon start.js",
    "db:test": "node -e \"import('pg').then(({Client})=>{const c=new Client({connectionString:process.env.PG_URI}); c.connect().then(()=>{console.log('connected'); c.end()}).catch(e=>{console.error(e.stack); process.exit(1)})}).catch(e=>{console.error(e && e.stack || e); process.exit(1)})\"",
    "db:up": "docker-compose up -d db",
    "db:down": "docker-compose down",
    "db:logs": "docker-compose logs -f db"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.15.2",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  }
}
